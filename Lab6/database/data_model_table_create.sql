CREATE TABLE public. (
    id integer NOT NULL,
    PRIMARY KEY (id)
);


CREATE TABLE public. (
    id integer NOT NULL,
    PRIMARY KEY (id)
);


CREATE TABLE public. (
    id integer NOT NULL,
    PRIMARY KEY (id)
);


CREATE TABLE public. (
);


CREATE TABLE public. (
);

